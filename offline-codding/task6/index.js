const user = [

    user1 = [
        name = 'Artem',
        age = '17',
        city = 'Kyiv',
        budget = 1000,
        ],

    user2 = [
        name = 'Arthur',
        age = '17',
        city = 'Kharkiv',
        budget = 1000,
        ],

    user3 = [
        name = 'Sergio',
        age = '17',
        city = 'Dnipro',
        budget = 1000,
    ],

];

function operation (name, age, city) {
    
    const newUser = user.map(elem => elem[1].age * 2)
    .reduce(acc, arg1, arg2, arg3, arg4), []) => );
}
console.log(user);